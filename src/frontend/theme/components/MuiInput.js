import { createPalette } from "../palette";
const { blue } = createPalette();
export const MuiInput = {
  styleOverrides: {
    root: {
      backgroundColor: "white",
      color: "black",
    },
  },
};
